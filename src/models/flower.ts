/**
 * Flower.
 * @class
 */
export class Flower {
  /**
   * Default constructor.
   * @param {string} name
   * @param {string} size
   */
  public name: string
  public size: string

  constructor(name: string, size: string) {
    this.name = name
    this.size = size
  }
}
