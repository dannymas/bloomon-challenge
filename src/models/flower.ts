/**
 * Flower.
 * @class
 */
export class Flower {
  /**
   * Default constructor.
   * @param {string} name
   * @param {string} size
   */
  name: string
  size: string

  constructor(name: string, size: string) {
    this.name = name
    this.size = size
  }
}
